package DataStructures.DS.LinkedList;

/**
 * @author Harish T
 */
public class Queue {
    private CircularLinkList linkList = new CircularLinkList();
    public int size = 0;

    public int dequeue(int data) {
        if (size == 0) return -1;
        return 0;
    }
}
